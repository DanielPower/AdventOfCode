{
  "type": "module",
  "devDependencies": {
    "prettier": "^3.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "markdown-table": "^3.0.3"
  },
  "scripts": {
    "test": "deno --allow-read --allow-write --allow-run --allow-env --allow-net --env test.ts"
  }
}
